define(['lib/pages', 'lib/draw', 'lib/event'], function(Page, Draw, Event) {
    return Page.extend({ 
        setup : function() {
            
        },
        draw : function() {
            
        }
    });
});